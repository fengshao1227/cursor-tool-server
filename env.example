# MySQL 数据库配置
# 方式1: 使用宿主机 MySQL（同服务器，不在容器中）- 推荐配置
DATABASE_URL=mysql://username:password@host.docker.internal:3306/license_db?timezone=Z
# 如果 host.docker.internal 不工作，可以尝试：
# DATABASE_URL=mysql://username:password@172.17.0.1:3306/license_db?timezone=Z  # Linux
# DATABASE_URL=mysql://username:password@docker.for.mac.localhost:3306/license_db?timezone=Z  # macOS 旧版本

# 方式2: 使用远程 MySQL（不同服务器）
# DATABASE_URL=mysql://username:password@remote-host:3306/license_db?timezone=Z

# 方式3: 使用本地 MySQL 容器（如果使用 docker-compose 中的 MySQL 容器）
# MYSQL_ROOT_PASSWORD=your_strong_mysql_password_here
# MYSQL_PORT=3306

# 应用配置
PORT=8080
JWT_SECRET=your_jwt_secret_at_least_32_chars_long_random_string

# Token 加密密钥（32位随机字符串）
ENCRYPTION_KEY=change_this_to_random_32_chars_key

# 管理员账号（首次启动时自动创建）
# 密码至少4位
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# CORS 配置（可选，如果前端单独部署）
ALLOW_ORIGIN=

