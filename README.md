# License Server

一个完整的许可证管理系统，包含后端 API 和管理面板。

## 🚀 快速开始

### 首次部署

```bash
# 1. 克隆代码
git clone <your-repo-url> license-server
cd license-server

# 2. 配置环境变量
cp env.example .env
nano .env  # 修改数据库连接、管理员账号等

# 3. 自动部署
chmod +x deploy.sh
./deploy.sh
```

### 日常更新

```bash
cd license-server
./deploy.sh
```

**就这么简单！** 脚本会自动：
- ✅ 拉取最新代码（git pull）
- ✅ 编译后端
- ✅ 构建前端
- ✅ 重启服务

🎉

## 📖 详细文档

- **完整部署指南**: [DEPLOY.md](./DEPLOY.md)
- **API 文档**: 查看源码 `src/routes/`
- **故障排查**: 见 DEPLOY.md

## ✨ 功能特性

- ✅ 许可证生成和管理
- ✅ 设备激活限制
- ✅ 过期时间管理
- ✅ 设备绑定/解绑
- ✅ Web 管理面板
- ✅ RESTful API
- ✅ 数字签名验证
- ✅ 审计日志

## 🔧 常用命令

```bash
./deploy.sh              # 部署/更新
./check-status.sh        # 检查状态
tail -f server.log       # 查看日志
pkill -f "node dist"     # 停止服务
```

## 📱 访问地址

- 管理后台: `http://your-server:8080/admin/`
- 健康检查: `http://your-server:8080/healthz`

## 🔐 默认账号

在 `.env` 文件中配置：
```bash
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123
```

## 📋 环境要求

- Node.js 18+
- MySQL 5.7+
- Git（用于代码更新）

## 🐳 Docker 部署（可选）

```bash
docker-compose up -d
```

## 📂 项目结构

```
license-server/
├── src/                 # 后端源码
│   ├── index.ts        # 入口文件
│   ├── routes/         # API 路由
│   ├── db.ts           # 数据库
│   └── config.ts       # 配置
├── admin/              # 前端管理面板
│   ├── src/
│   │   ├── App.tsx    # 主组件
│   │   └── main.tsx   # 入口
│   └── dist/          # 构建输出
├── deploy.sh          # 部署脚本
├── check-status.sh    # 状态检查
├── init-db.sql        # 数据库初始化
└── .env               # 环境变量（不提交）
```

## 🛠️ 技术栈

**后端:**
- Node.js + TypeScript
- Express - Web 框架
- MySQL - 数据库
- JWT - 身份认证
- bcryptjs - 密码加密

**前端:**
- React
- Vite - 构建工具
- Axios - HTTP 客户端

## 📄 License

MIT License

---

**遇到问题？** 查看 [DEPLOY.md](./DEPLOY.md) 或查看日志 `tail -f server.log`
